rules:
  execguard:
    banned:
      - pkg: "os/exec"
        except: ["allowed"]
        reason: "use subprocess"
      - pkg: "os"
        methods: ["Getenv"]
        except: ["allowed"]
        reason: "use config"
